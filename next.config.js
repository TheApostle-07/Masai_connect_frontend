/** @type {import('next').NextConfig} */
const nextConfig = {
    // Enable static export
    output: 'export',
    // Optionally force trailing slashes for static export
    trailingSlash: true,
  };
  
  module.exports = nextConfig;